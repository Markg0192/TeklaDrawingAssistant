<Window x:Class="TeklaDrawingAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tekla Drawing Assistant"
        Width="820" Height="650"
        MinWidth="720" MinHeight="560"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Tekla Drawing Assistant" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock Text="Face-aware fabrication drawing setup and dimensioning for Tekla Structures 2023."
                       Margin="0,4,0,0" Foreground="DimGray"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Connection" FontWeight="SemiBold"/>
                    <TextBlock x:Name="ConnectionText" Text="Checking Tekla..." Margin="0,3,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="RefreshButton" Content="Refresh" Padding="16,6" Click="RefreshButton_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="AnalyzeButton" Content="Analyze Drawing" Padding="18,8" Margin="0,0,10,0" Click="AnalyzeButton_Click"/>
                <Button Grid.Column="1" x:Name="DimensionButton" Content="Tidy Drawing" Padding="18,8" Margin="0,0,18,0" Click="DimensionButton_Click"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,18,0">
                    <TextBlock Text="Min offset (mm)" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="OffsetTextBox" Text="25" Width="52" Padding="4"/>
                </StackPanel>

                <StackPanel Grid.Column="3" VerticalAlignment="Center">
                    <CheckBox x:Name="DeleteExistingCheckBox" Content="Delete existing straight dimensions before rebuilding"/>
                    <CheckBox x:Name="SaveDrawingCheckBox" Content="Save drawing after run" IsChecked="True" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <TextBox Grid.Row="3" x:Name="LogTextBox"
                 FontFamily="Consolas" FontSize="13"
                 IsReadOnly="True" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"/>
    </Grid>
</Window>
